{
    "name": "panopticon-appwrite-function",
    "version": "1.0.0",
    "description": "Appwrite function to receive POST requests and store data in database",
    "main": "index.js",
    "type": "module",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "node-appwrite": "^14.0.0"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}